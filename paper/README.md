# paper

Paper drafts and submission assets live here. Figures generated by
`make figures` (via `scripts/make_figures.py`) are written to
`paper/figures/`.

See `README.md` (repo root) for the submission deadline.
